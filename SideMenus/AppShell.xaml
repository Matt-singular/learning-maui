<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:SideMenus"
       x:Class="SideMenus.AppShell"
       x:DataType="local:AppShell"
       FlyoutBehavior="Locked">

    <!-- Currently the flyout items have been added progammatically in the code behind, but below is the xaml implementation -->
    
    <!--<FlyoutItem Title="About" Icon="icon_home.png">
        <ShellContent Route="AboutPage" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>-->
    
    <!--<FlyoutItem Title="Home" Icon="icon_home.png">
    <ShellContent Route="HomePage" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>-->

</Shell>